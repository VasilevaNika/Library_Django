# docker-compose.yml
# Версия формата Compose
version: '3.8'

# Секция services описывает все контейнеры
services:
  # --- Auth Service ---
  auth-service:
    # Сборка из Dockerfile в папке ./auth-service
    build: ./auth-service
    # Проброс портов: порт хоста 8001 -> порт контейнера 8000
    ports:
      - "8001:8000"
    # Монтируем код для разработки
    volumes:
      - ./auth-service:/app
    # Переменные окружения (для будущего подключения БД)
    environment:
      - DATABASE_URL=postgresql://postgres:password@auth-db:5432/authdb
    # Зависимости: сначала запускается БД
    depends_on:
      - auth-db
    # Имя сети (все сервисы в одной сети видят друг друга)
    networks:
      - backend

  # База данных для Auth-сервиса
  auth-db:
    image: postgres:15
    environment:
      - POSTGRES_DB=authdb
      - POSTGRES_PASSWORD=password
    volumes:
      - auth-db-data:/var/lib/postgresql/data
    networks:
      - backend

  # --- Review Service ---
  review-service:
    build: ./review-service
    ports:
      - "8002:8000"
    volumes:
      - ./review-service:/app
    environment:
      - DATABASE_URL=postgresql://postgres:password@review-db:5432/reviewdb
      # ВАЖНО: URL Auth-сервиса для вызовов из Review
      # Внутри сети Docker имя auth-service преобразуется в IP
      - AUTH_SERVICE_URL=http://auth-service:8000
      - NOTIFICATION_SERVICE_URL=http://notification-service:8000
    depends_on:
      - auth-service  # Review зависит от Auth
    networks:
      - backend

  review-db:
    image: postgres:15
    environment:
      - POSTGRES_DB=reviewdb
      - POSTGRES_PASSWORD=password
    volumes:
      - review-db-data:/var/lib/postgresql/data
    networks:
      - backend

  # --- Notification Service ---
  notification-service:
    build: ./notification-service
    ports:
      - "8003:8000"
    volumes:
      - ./notification-service:/app
    networks:
      - backend

# Определяем сети
networks:
  backend:
    driver: bridge

# Определяем volumes для сохранения данных БД
volumes:
  auth-db-data:
  review-db-data:
